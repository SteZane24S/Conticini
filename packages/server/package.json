{
  "name": "@conticini/server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@conticini/contratti": "*",
    "@conticini/dominio": "*",
    "better-sqlite3": "12.10.0",
    "fastify": "^5.6.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0"
  }
}
